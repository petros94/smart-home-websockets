# Websocket-ActiveMQ-demo
Websocket client-server example app, with ActiveMQ message broker


## How to install

From root directory type:

> mvn package -DskipTests

To build the docker images run:

> docker-compose build

